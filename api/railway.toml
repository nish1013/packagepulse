[build]
builder = "RAILPACK"
watchPatterns = ["/api/**"]

[deploy]
startCommand = "uvicorn packagepulse.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
